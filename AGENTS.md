# AGENTS.md

The guidance for this repository lives in **[CLAUDE.md](CLAUDE.md)**, and it applies to
every agent, not only to Claude Code. Read it before touching anything: the commands, the
four layers, the invariants a change can silently break, and the rule that everything
committed is written in English.

Kept as a pointer rather than a copy, so the two can never drift apart.
